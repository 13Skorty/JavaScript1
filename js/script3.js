const text = 'To add a script to a web page, the script tag is used in the HTML file, in the src attribute of which we specify a link to an external JavaScript file.';
console.log(text)
console.log(text.length) 